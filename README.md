#Curl


So easy as

//only curl include with
#include "Curl.h"

//get example&nbsp
int main() {&nbsp
	Curl curl;&nbsp
	curl.get("https://google.com");&nbsp
	std::string = curl.response();&nbsp
}&nbsp

